"use client";

import Clock from "../Clock";

const ClockWrapper = () => {
  return (
    <section className="flex gap-1 items-center">
      <Clock />
    </section>
  );
};

export default ClockWrapper;
